import 'package:flutter/material.dart';

class SearchRoadsView extends StatelessWidget {
  const SearchRoadsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}