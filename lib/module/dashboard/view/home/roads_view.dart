import 'package:flutter/material.dart';

class RoadsView extends StatelessWidget {
  const RoadsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}