import 'package:flutter/material.dart';

class SearchUtilitiesView extends StatelessWidget {
  const SearchUtilitiesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}